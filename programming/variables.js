/* variables>>theree are three unique namiong varibles used in javascript 
they are storage components that are used to store assigned values 
they include {
    var>>older variabe declaration,they can be redeclared in a code base under function scope

    let>>newest modern variable declaratrion that has block scope reassignment 

   

    const>elements and values do not change after assignment >>cannot be redeclared

       naming conventions variable name =value
} */

       var myName="Peter"

       let age=345

       const PI=3.14

       console.log(myName);
       console.log(age);
       console.log(PI);

       myName="stephen"

       age=456

       

       console.log(myName);
       console.log(age);
       console.log(PI);



       

